apiVersion: v1
kind: Pod
metadata:
  name: "{{ .Release.Name }}-test-scorer"
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: test
      image: registry.access.redhat.com/ubi9/ubi-minimal@sha256:34880b64c07f28f64d95737f82f891516de9a3b43583f39970f7bf8e4cfa48b7
      command: ["sh", "-c"]
      args:
        - |
          set -eu
          base_url="http://{{ .Release.Name }}-scorer:8000"
          curl -sf "${base_url}/ready"
          curl -sf -X POST "${base_url}/api/v1/score" \
            -H "Content-Type: application/json" \
            -d '{"amount": 50, "country": "US", "category": "retail"}'
          echo "Scorer smoke test: OK"
  restartPolicy: Never
